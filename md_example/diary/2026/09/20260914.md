# 2026-09-14 (Mon)

## Todo
- [ ] QTextEdit: 先頭・末尾に行挿入＊１万行に要する処理時間計測してみる
- [ ] 見出し＋本文＊２４行を先頭・末尾に insertMarkdown() する処理時間計測してみる

## Notes
### 先頭付近文字挿入時 インクリメンタル更新 処理時間
```CSV
lines, t1, t2, t3, t4, t5, ave, std
200, 13, 12, 13, 12, 13, 12.60, 0.55
500, 25, 27, 23, 25, 28, 25.60, 1.95
1000, 48, 48, 50, 47, 46, 47.80, 1.48
2000, 149, 112, 114, 117, 113, 121.00, 15.76
5000, 282, 287, 283, 303, 281, 287.20, 9.12 
```

### 先頭付近文字挿入時 インクリメンタル更新（改） 処理時間
```CSV
lines, t1, t2, t3, t4, t5, ave, std
200, 3, 3, 2, 3, 3, 2.80, 0.45
500, 4, 4, 3, 3, 5, 3.80, 0.84
1000, 5, 5, 4, 5, 5, 4.80, 0.45
2000, 6, 7, 7, 7, 8, 7.00, 0.71
5000, 7, 8, 7, 11, 7, 8.00, 1.73
10000, 11, 12, 13, 12, 14, 12.40, 1.14
```
### 末尾付近文字挿入時 インクリメンタル更新 処理時間
```CSV
lines, t1, t2, t3, t4, t5, ave, std
200, 5, 5, 5, 6, 6, 5.50, 0.53
500, 6, 7, 6, 6, 7, 6.50, 0.53
1000, 7, 6, 7, 7, 6, 7.00, 0.94
2000, 7, 6, 6, 5, 5, 5.40, 0.70
5000, 6, 6, 7, 6, 7, 6.40, 0.55 
10000, 17, 5, 6, 5, 20, 10.6, 7.3
```

### 末尾付近文字挿入時 全体更新 処理時間
```CSV
lines, t1, t2, t3, t4, t5, ave, std
200, 18, 19, 22, 17, 19, 19.00, 1.87
500, 37, 39, 37, 41, 41, 39.00, 2.00
1000, 66, 64, 63, 67, 64, 64.80, 1.64
2000, 124, 118, 127, 122, 126, 123.40, 3.58
5000, 304, 305, 309, 307, 312, 307.40, 3.21 
```

### QTextEdit へ文字列挿入する処理時間を計測してみた。

空文書にて、下記の処理時間は522msecだった。
続けて同じ処理を繰り返すと1059msecだった。
続けて同じ処理を繰り返すと1906msecだった。
おおむね文書行数に比例している
```
  const int N_LOOP = 1000;
  const QString g_text = "body text body text body text body text body text body text body text body text \n";
  QTextCursor cursor = docWidget->m_preview->textCursor();
  for(int i = 0; i < N_LOOP; ++i) {
      cursor.movePosition(QTextCursor::Start);
      cursor.insertText(g_text);
  }
  docWidget->m_preview->setTextCursor(cursor);
```

下記のように、beginEditBlock(), endEditBlock() で囲った場合、
空文書にて、下記の処理時間は48だった。
続けて同じ処理を繰り返すと50だった。
続けて同じ処理を繰り返すと49だった。
文書行数に比例していないように見える

```
  const int N_LOOP = 1000;
  const QString g_text = "body text body text body text body text body text body text body text body text \n";
  QTextCursor cursor = docWidget->m_preview->textCursor();
  cursor.beginEditBlock();
  for(int i = 0; i < N_LOOP; ++i) {
      cursor.movePosition(QTextCursor::Start);
      cursor.insertText(g_text);
  }
  cursor.endEditBlock();
  docWidget->m_preview->setTextCursor(cursor);
```
