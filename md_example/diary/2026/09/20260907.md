# 2026-09-07 (Mon)

## Todo
- [ ] エディタ編集時：見出し＋本文 単位インクリメンタル更新

## Notes
現状、setMarkdown() コール元：
- ロード時：
  - do\_open()
    -   MainWindow::addTab()
        - mdEditor\->setPlainText(txt);
          - MainWindow::onMDTextChanged()
        - MarkdownEditor::onContentsChanged(int, int, int)
- 編集時：
  - MarkdownEditor::do\_insertText()
    - cursor.insertText(txt);
      - MainWindow::onMDTextChanged()
      - MarkdownEditor::onContentsChanged(int, int, int)

